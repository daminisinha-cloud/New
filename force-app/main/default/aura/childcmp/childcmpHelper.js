({
	/*getrecord : function(component, event, obj,data) {
		 var action = component.get("c.fetchrecord");
       console.log(obj)
       action.setParams({
            'objname': obj
          }); 
        action.setCallback(this, function(response) {
            var state = response.getState();
            if (state === "SUCCESS") {
                var storeResponse = response.getReturnValue(); console.log(storeResponse)
               alert('record4')
                component.set("v.data", storeResponse);console.log(component.get("v.data")+'apex data')
            }
 
        }); 
        $A.enqueueAction(action);
    
	}*/
	
})